# default-initial-app (Flutter)

**Status:** em preparação.

Este pacote será usado com:

```bash
curl .../init.sh | bash -s -- --profile app --with-descritivo
```

Estrutura prevista:

```
lib/
docs/
pubspec.yaml
```

Contribua em `base/` quando o template Flutter estiver definido pelo time.
